# SD-AI Artifact Commit Intent — SIGN-001

This branch intentionally tracks the feature lifecycle artifacts required for review and traceability: specification, architecture, ADRs, security review, implementation plan, task graph, implementation brief, and repository-readable evidence.

The feature evidence under `specs/SIGN-001/evidence/` is committed so reviewers can inspect current verification status in Git. Local SD-AI worktree metadata, when produced by a local `sdai run`, remains Git metadata and is not represented as if it were generated by this connector session.
